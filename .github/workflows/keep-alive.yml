name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site to prevent Render cold start
        run: curl -sSf --max-time 60 --retry 2 --retry-delay 5 https://yozeum-test.onrender.com/ -o /dev/null
